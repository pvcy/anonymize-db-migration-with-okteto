{
  "scripts": {
    "start": "node server.js",
    "load-data": "node load-data.js",
    "psql": "PGPASSWORD=$DB_PASSWORD psql -h $DB_HOST -U $DB_USERNAME",
    "migrate": "npx sequelize db:migrate",
    "migrate:undo": "npx sequelize db:migrate:undo"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.33.0"
  },
  "devDependencies": {
    "sequelize-cli": "^6.6.1"
  }
}
